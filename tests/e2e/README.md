TASK: Create the first Playwright web-E2E increment as NEW files only (do not edit existing files): 
1. e2e/web/playwright.config.ts — export a Playwright config: testDir '.', use.baseURL = process.env.E2E_BASE_URL || 'http://localhost:3000', a single 'chromium' project (import { defineConfig, devices } from '@playwright/test'), timeout 30000, retries 1.
2. e2e/web/dashboard.spec.ts — import { test, expect } from '@playwright/test'; a test 'dashboard loads and renders' that does: await page.goto('/dashboard'); expect the response status to be OK; await expect(page.locator('body')).toBeVisible(); and assert the page has some visible text (e.g. await expect(page.locator('body')).not.toBeEmpty() OR check a heading). Keep it resilient (no brittle selectors).
3. e2e/web/README.md — short: how to run (`pnpm dlx playwright install chromium` then `E2E_BASE_URL=https://storyagent.pbradygeorgen.com pnpm dlx playwright test -c e2e/web/playwright.config.ts` for deployed, or default localhost with `pnpm dev` running). Note it's the first increment; more routes (/observation-lounge, /crew/memories, /agent, /innovation-lounge) come next.
Do NOT run playwright (browsers not installed). Do NOT run git. These are additive scaffolding files; no build/typecheck needed (they import @playwright/test which may not be installed yet — that's expected).