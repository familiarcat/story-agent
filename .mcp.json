{
  "mcpServers": {
    "story-agent": {
      "command": "scripts/mcp-crew-stdio.sh",
      "comment": "Local stdio tool bridge. VS Code extension chat is cloud-first with a local HTTP fallback; use scripts/setup-hosted-mcp.sh only after STORY_AGENT_MCP_BEARER is available."
    },
    "supabase": {
      "type": "http",
      "url": "https://mcp.supabase.com/mcp?project_ref=rpkkkbufdwxmjaerbhbn",
      "headers": {
        "Authorization": "Bearer ${SUPABASE_ACCESS_TOKEN}"
      }
    },
    "aha": {
      "type": "http",
      "url": "https://familiarcat.use3.aha.io/api/v1/mcp",
      "headers": {
        "Authorization": "Bearer ${AHA_API_KEY}"
      }
    },
    "figma": {
      "type": "http",
      "url": "http://127.0.0.1:3845/mcp",
      "comment": "Optional local Figma desktop endpoint; start Figma Dev Mode only when design inspection is needed."
    }
  }
}
