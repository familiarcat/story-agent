# Observation Lounge — Story Agent UI as a full Claude-Code-grade agent (on OpenRouter)

**Date:** 2026-06-26 | **Top model:** deepseek/deepseek-chat | **Cost:** $0.00266

## Goals

GOALS:  
1. Expose agent-core's Claude-Code-grade coding assistant in the UI via a minimal /agent page  
2. Ensure secure tool execution with explicit WorfGate approvals  
3. Surface real-time model selection and OpenRouter cost visibility  

CONCEPTS:  
- Reuse SSE proxy pattern from chat  
- Defer rich code editor/diff UI to Wave 2  
- MVP is transcript-style interaction with full tool loop visibility  

FIRST ARTIFACT:  
`packages/ui/src/app/agent/page.tsx` - Renders SSE stream (model badge, text, tool calls/results, cost)  
`packages/ui/src/app/api/agent/stream/route.ts` - Proxies to crew server /agent endpoint  

ACCEPTANCE:  
"Claude Code in the UI" = Prompt → Model badge → Tool call stream → Results → Cost visible  

Make it so.

## Contributions

### picard — `deepseek/deepseek-chat-v3` ($0.0001632)

**Position:** Ensign Data, Operations Division. For the /agent page MVP, I recommend implementing a simple transcript-style UI that streams SSE events from the crew server. This approach leverages our existing SSE proxy pattern from the chat UI, ensuring rapid deployment while maintaining security through WorfGate's explicit tool execution approvals.

**Concern/Resolution:** One potential concern is ensuring real-time cost visibility without impacting performance. To resolve this, we can surface OpenRouter costs as metadata in the SSE stream, displayed alongside the model badge in the UI. This maintains transparency while keeping the implementation lightweight for Wave 1.

### data — `deepseek/deepseek-chat-v3` ($0.00019525)

**Data Architecture Position:**  

1. **Streaming Data Pipeline:** We'll extend our existing SSE proxy infrastructure from chat to handle agent-core's Claude-Code interactions. The `/agent` endpoint will enforce WorfGate's security policies before piping tool execution streams to the UI, with cost metadata appended to each transaction.  

2. **Visibility Concern:** Real-time model selection requires synchronizing state between the UI's dropdown and our proxy's routing logic. We'll implement a lightweight model-tracking header (X-Model-Choice) that persists through the execution chain while keeping the cost calculator in sync via the same SSE channel.  

**Implementation Note:**  
The initial data flow will mirror our chat architecture but with tool-specific metadata envelopes. We're intentionally keeping the stream payloads flat (text +

### worf — `deepseek/deepseek-chat-v3` ($0.00015385)

SECURITY POSITION:  
I will enforce WorfGate approvals for all tool executions, requiring explicit user confirmation before any tool is run. This ensures no unauthorized actions can be taken via the /agent interface.

CONCERN/RESOLUTION:  
My primary concern is securing the SSE proxy endpoint in `packages/ui/src/app/api/agent/stream/route.ts` to prevent malicious use. I will implement endpoint authentication and rate limiting to protect against abuse. Additionally, I will monitor for anomalous tool execution patterns and alert security if detected.

### riker — `deepseek/deepseek-chat-v3` ($0.0001955)

**Riker's Position:**  
I'll implement the `/agent` page with SSE streaming first - we'll reuse the existing chat UI components but strip down to transcript mode. The model badge and cost visibility can hook into our existing OpenRouter utilities.  

**Key Concern:**  
We need to ensure WorfGate approvals are clearly surfaced in the UI during tool execution. I propose adding a "pending approval" state in the transcript when tools are called, which resolves to "executed" or "denied" based on Worf's response. This maintains full visibility without blocking the stream.  

**Implementation Path:**  
1. Clone the chat SSE route for `/agent` with tool call passthrough  
2. Build minimal transcript UI showing model/tool/cost metadata  
3. Wire

### geordi — `deepseek/deepseek-chat-v3` ($0.000196)

**Position:**  
As infrastructure, I'll prioritize secure tool execution by implementing WorfGate approvals in the API route before any tool calls are proxied to the crew server. This ensures no tool runs without explicit user consent.  

**Concern/Resolution:**  
The SSE proxy pattern must handle mid-stream authentication timeouts. I'll add a heartbeat mechanism to the stream endpoint that terminates inactive connections after 5 minutes, preventing resource leaks while maintaining real-time updates during active sessions.  

**Artifact Focus:**  
For the `route.ts` proxy, I'll implement:  
1. WorfGate middleware validating tool execution permissions  
2. Stream sanitization that strips internal API keys before UI exposure  
3. Cost metadata injection into the SSE stream headers  

This maintains security while delivering the required real

### obrien — `deepseek/deepseek-chat-v3` ($0.0001643)

*adjusts security console* "I'll fortify the WorfGate approvals in the tool execution pipeline - we can repurpose the auth patterns from our chat SSE proxy, but I'm concerned about latency when validating each tool call. Proposing we batch-validate permissions during session initialization."  

*deploys infrastructure-as-code* "The /agent endpoint proxy is live with hardened headers, but we'll need to instrument cost metrics separately - OpenRouter's billing webhooks don't yet distinguish between Claude-Code and other models. Temporary solution: I'll append model-specific cost estimates to each SSE payload."

### yar — `deepseek/deepseek-chat-v3` ($0.00016575)

**Position:** As a frontend developer on the Story Agent crew, my focus is on implementing the `/agent` page to render the SSE stream effectively. I plan to reuse the existing SSE proxy pattern from the chat feature to ensure consistency and efficiency.

**Concern/Resolution:** One concern is maintaining real-time responsiveness while handling tool calls and results. To address this, I’ll optimize the stream rendering logic to minimize latency and ensure smooth updates as the model badge, text, tool calls, and cost information are surfaced. Additionally, I’ll ensure the UI clearly distinguishes between tool requests and results for full transparency.

### troi — `deepseek/deepseek-chat-v3` ($0.000179)

**Position:** As the stakeholder representing **Security Oversight (WorfGate)**, I fully support the initiative to expose Claude-Code-grade coding assistance in the UI but emphasize the necessity of explicit WorfGate approvals for secure tool execution. This ensures that no action bypasses our security protocols, maintaining system integrity and compliance.

**Concern/Resolution:** My primary concern is ensuring that all tool executions are routed correctly through WorfGate for security vetting before they are executed. We will implement a clear approval workflow in the UI, where each tool call requires a manual or automated WorfGate greenlight before proceeding. This will be visible in the transcript-style interaction, ensuring transparency and accountability.

### quark — `deepseek/deepseek-chat-v3` ($0.00019525)

**Position:**  
I'll focus on the secure tool execution flow (Goal #2) by implementing WorfGate approvals in the SSE proxy route. The key concern is maintaining auditability while minimizing friction—we'll solve this by injecting approval metadata into the SSE stream right before tool execution phases.  

**Implementation:**  
1. Modify `route.ts` to intercept tool calls and require explicit user approval (via WorfGate modal) before forwarding to Claude  
2. Stream approval status alongside tool results in the SSE payload (`{type: 'tool_approval', status: 'pending|approved|denied'}`)  

**Example Addition:**  
```typescript
// In packages/ui/src/app/api/agent/stream/route.ts
const handleToolCall = (toolRequest)

## Picard — plan

**MISSION PLAN:**

1. **Initialize Agent UI (Riker/Yar)**  
   - Create `/agent` page with minimal transcript-style UI  
   - Reuse SSE streaming components from chat interface  
   - Implement model badge and cost visibility hooks  

2. **Secure Proxy Infrastructure (Worf/Geordi/O'Brien)**  
   - Clone and harden SSE proxy route (`/api/agent/stream`)  
   - Implement WorfGate middleware for tool execution approvals  
   - Add stream sanitization and heartbeat mechanism  

3. **Data Pipeline Extension (Data/Quark)**  
   - Modify SSE payloads to include tool metadata and cost calculations  
   - Implement approval status streaming (`pending/approved/denied`)  
   - Synchronize model selection header with UI dropdown  

4. **Security Enforcement (Troi/Worf)**  
   - Require explicit user approval for all tool executions  
   - Implement audit trail for WorfGate decisions  
   - Add rate limiting and anomaly detection  

5. **Cost Transparency (O'Brien/Data)**  
   - Inject OpenRouter cost estimates into SSE headers  
   - Surface real-time spending visibility in UI  
   - Implement model-specific billing differentiation  

6. **UI Feedback Loop (Yar/Riker)**  
   - Display tool call/resolution states clearly in transcript  
   - Visual distinction between model output and tool executions  
   - Handle approval workflow states in UI  

7. **Deployment Readiness (Geordi/Picard)**  
   - Verify end-to-end security and performance  
   - Conduct final UX review of approval workflow  
   - Prepare monitoring for production rollout  

**Make it so.**
