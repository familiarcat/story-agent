FROM node:22-alpine
# git: required by agent-core/loop.ts buildEnvContext() and tools.ts git_status/git_diff handlers
# ripgrep (rg): required by search_code/glob_files tools
# openssh-client: required for git push over SSH from inside the container
RUN apk add --no-cache git ripgrep openssh-client bash
WORKDIR /app
